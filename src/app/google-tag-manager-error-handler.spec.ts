import { GoogleTagManagerErrorHandler } from './google-tag-manager-error-handler';

describe('GoogleTagManagerErrorHandler', () => {
  it('should create an instance', () => {
    expect(new GoogleTagManagerErrorHandler()).toBeTruthy();
  });
});
