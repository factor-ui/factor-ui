import { GoogleTagErrorHandler } from './google-tag-error-handler';

describe('GoogleTagErrorHandler', () => {
  it('should create an instance', () => {
    expect(new GoogleTagErrorHandler()).toBeTruthy();
  });
});
