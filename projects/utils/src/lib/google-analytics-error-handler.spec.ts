import { GoogleAnalyticsErrorHandler } from './google-analytics-error-handler';

describe('GoogleAnalyticsErrorHandler', () => {
  it('should create an instance', () => {
    expect(new GoogleAnalyticsErrorHandler()).toBeTruthy();
  });
});
